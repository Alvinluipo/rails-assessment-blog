<%= form_for(@comment) do |f| %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>

  <p>
    <%= f.hidden_field :post_id, value: @post.id %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>