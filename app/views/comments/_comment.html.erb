<strong><%= comment.name %></strong></p>
<%= comment.body %><p>
<h5><%= time_ago_in_words(comment.created_at) %> Ago </h5>


  <% if current_user %>
    <% if comment.user_id == current_user.id %>
      <p><%= link_to 'Delete', [comment],
                      method: :delete,
                      class: "botton",
                      data: { confirm: 'Are you sure?' } %>
      </p>
    <% end %>
  <% end %> 
</div>