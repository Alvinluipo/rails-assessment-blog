<!DOCTYPE html>
<html>
<head>
  <title>RailsAssessmentBlog</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id="main_content">
  <strong><h1>Welcome to Alvin's Blog Post</h1></strong>
    <div id="header">
      <div class="buttons">
        <button class="button"><%= link_to "Back to All Posts", posts_path %></button>
        <button class="button"><%= link_to("Sign out", destroy_user_session_path, :method => "delete") %></button>
        <button class="button"><%= link_to("Sign in", new_user_session_path) %></button>
        <button class="button"><%= link_to("Sign up", new_user_registration_path) %></button>
        <button class="button"><%= link_to("Show posts by Tags", tags_path) %></button>
     
      


      <% if user_signed_in? %>
        <div class="buttons">
          <button class="button"><%= link_to "New Post", new_post_path %></button>
          <button class="button"><%= link_to("My post", user_posts_path(current_user)) %></button>
          <button type="button" id="all_comments">all comments</button>
        </div>
      <% end %>
    </div>

    <% flash.each do |name, msg| %>
      <%= content_tag(:div, msg, class: "alert") %>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>

